package com.example.applicationtp4_ex2;import android.os.Bundle;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import android.view.WindowManager;import android.widget.Button;import android.widget.EditText;import androidx.annotation.Nullable;import androidx.fragment.app.DialogFragment;public class carteFragment extends DialogFragment {    private EditText Nom_Prenom;    private EditText Num_Carte;    private EditText cvc;    private EditText date;    private SimpleDialogListener listener;    private Button btn;    public carteFragment() {        // le fragment est créé par la méthode newInstance    }    public static carteFragment newInstance(String title) {        carteFragment frag = new carteFragment();        Bundle args = new Bundle();        args.putString("title", title);        frag.setArguments(args);        return frag;    }    @Override    public View onCreateView(LayoutInflater inflater, ViewGroup container,                             Bundle savedInstanceState) {        return inflater.inflate(R.layout.fragment_simple_dialog_carte, container);    }    @Override    public void onViewCreated(View view, @Nullable Bundle savedInstanceState) {        super.onViewCreated(view, savedInstanceState);        listener = (SimpleDialogListener) getActivity();        Nom_Prenom = (EditText) view.findViewById(R.id.txt_pseudo);        Num_Carte = (EditText) view.findViewById(R.id.editTextNumber);        cvc = (EditText) view.findViewById(R.id.editTextNumber2);        date = (EditText) view.findViewById(R.id.txt_dateExpiration);        btn = (Button) view.findViewById(R.id.ok);        // quand le button est cliqué, l'activité est appellé,        // la valeur mEditText est passeé à l'activité en paramètre        btn.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                listener.onOkClickDialog(Nom_Prenom.getText().toString());            }        });        String title = getArguments().getString("title", "Votre nom");        getDialog().setTitle(title);        Nom_Prenom.requestFocus();        getDialog().getWindow().setSoftInputMode(WindowManager.LayoutParams.SOFT_INPUT_STATE_VISIBLE);    }}